# API Configuration
# Replace with actual values, but NEVER commit the real .env file
API_URL=https://api.example.com
API_TIMEOUT=5000

# Email Service (for contact form)
EMAIL_SERVICE=example_service
EMAIL_USER=your_username
EMAIL_PASS=your_password
EMAIL_TO=recipient@example.com

# Analytics
ANALYTICS_ID=UA-XXXXXXXXX-X

# Feature Flags
ENABLE_FEATURE_X=false
ENABLE_ANALYTICS=false

# Note: Never store sensitive information in client-side code
# For secrets that need to be accessed from the client, use server-side APIs